import { Grid2 } from "@mui/material";
import styled from "styled-components";

export const MorePageWrapper = styled(Grid2)(() => ({
    flexDirection: "column", 
    width: "100vw",
    height: "100vh"
}))
